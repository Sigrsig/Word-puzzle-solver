import React from "react";
import { render, screen } from "@testing-library/react";
import Main from "./Main";

describe("Main", () => {
  // Spent an hour and a half dealing with eslint and reading up on doing tests on multiple inputs.. I'm just gonna start with TDD on the calculations instead
  it.todo("should be possible to enter 12 letters in the textboxes");

  it.todo("should not be possible to input numbers");

  it.todo("should not be possible to have an empty input");
});
